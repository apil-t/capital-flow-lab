[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "capital-flow-lab"
version = "0.1.0"
description = "Point-in-time research on disclosed Indian equity fund holdings"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
pilot = ["openpyxl>=3.1,<4"]

[project.scripts]
flowrank = "flowrank.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
